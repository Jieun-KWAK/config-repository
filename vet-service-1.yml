spring:
  application:
    name: vet-service-1  #?? ??? git ???? ??? .yml ??? ??? ??

  #spring cloud ?? ?? ??
  config:
    import: "optional:configserver:"
    cloud:
      config:
        uri: http://localhost:8888 #config server? url